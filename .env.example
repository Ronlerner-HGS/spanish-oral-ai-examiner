# Groq API Key for LLM and Whisper transcription
GROQ_API_KEY=your_groq_api_key_here

# Inworld API Key for Text-to-Speech (Base64 encoded)
# Get your API key from: https://platform.inworld.ai/ -> Settings -> API Keys
INWORLD_API_KEY=your_inworld_base64_api_key_here

# Optional: Custom Inworld voice ID (defaults to Rafael)
# Available voices: Rafael, Ashley, Dennis, and many more - see https://docs.inworld.ai/docs/tts/tts-voices
# INWORLD_VOICE_ID=Rafael

# MongoDB connection URL for session storage
# 
# RECOMMENDED: MongoDB Atlas (free 512MB tier)
# 1. Go to https://www.mongodb.com/atlas and create free account
# 2. Create a free M0 cluster (choose closest region)
# 3. Database Access: Create a database user with password
# 4. Network Access: Add 0.0.0.0/0 to allow all IPs (required for Railway)
# 5. Connect > Drivers > Copy connection string
# 6. Replace <password> with your database user's password
#
# Example Atlas URL:
# MONGO_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/spanish-tutor?retryWrites=true&w=majority
#
# For Railway: Set this in your service variables (not in .env file)
MONGO_URL=

# Server port (defaults to 3000)
# PORT=3000
